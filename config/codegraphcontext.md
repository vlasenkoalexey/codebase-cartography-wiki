---
slug: codegraphcontext
repo: https://github.com/CodeGraphContext/CodeGraphContext
acquire: submodule
synthesis_focus: >-
  Code comprehension — how CodeGraphContext turns a codebase into a graph an LLM
  can query: its indexing/parsing pipeline, the graph model it builds (nodes,
  edges, call/import/inheritance relationships) and the store it lives in, how it
  exposes that graph to an agent (MCP server / tools), and how it keeps the graph
  current (watching, re-indexing). Emphasize what makes it comparable to the other
  surveyed tools (wikify-repo, graphify, understand-anything) for the cross-repo
  concept pages — grounding substrate, symbol/call graph, and the query interface.
---

## Concepts
<!-- Auto-seeded from code centrality. Add seed concepts here to go deeper into a subsystem. -->
